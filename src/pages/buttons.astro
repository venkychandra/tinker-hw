---
import GalleryLayout from '../layouts/GalleryLayout.astro';
import Button from '../components/Button.astro';
import ArrowRight from '../components/icons/ArrowRight.astro';
import Search from '../components/icons/Search.astro';
import Menu from '../components/icons/Menu.astro';
import Close from '../components/icons/Close.astro';
import MapPin from '../components/icons/MapPin.astro';
import Calendar from '../components/icons/Calendar.astro';
---

<GalleryLayout title="Buttons" description="Three variants, three sizes, full state coverage. Buttons use inline-flex layout with consistent padding ratios and a 1px active press." fullscreenHref="/view/buttons">

  <!-- Variants -->
  <section class="specimen-section">
    <h2>Variants</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">primary</code> for the main call-to-action,
      <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">secondary</code> for supporting actions (inset border, no fill),
      <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">ghost</code> for tertiary or de-emphasized actions.
    </p>
    <div class="border border-gray-200 rounded-lg p-8">
      <div class="flex flex-wrap gap-6 items-center">
        <div class="flex flex-col items-center gap-3">
          <Button>Book This Trip</Button>
          <span class="text-xs font-mono text-gray-400">primary</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button variant="secondary">View Itinerary</Button>
          <span class="text-xs font-mono text-gray-400">secondary</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button variant="ghost">Back to Trips</Button>
          <span class="text-xs font-mono text-gray-400">ghost</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Sizes -->
  <section class="specimen-section">
    <h2>Sizes</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      Three size tiers. Padding scales disproportionately — larger buttons get more generous horizontal padding. Border radius shifts from <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">rounded-sm</code> (4px) at small to <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">rounded-md</code> (6px) at medium and large.
    </p>
    <div class="border border-gray-200 rounded-lg p-8 mb-6">
      <div class="flex flex-wrap gap-8 items-end">
        <div class="flex flex-col items-center gap-3">
          <Button size="sm">Small</Button>
          <span class="text-xs font-mono text-gray-400">sm</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button>Medium</Button>
          <span class="text-xs font-mono text-gray-400">md (default)</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button size="lg">Large</Button>
          <span class="text-xs font-mono text-gray-400">lg</span>
        </div>
      </div>
    </div>

    <!-- Size reference table -->
    <div class="overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="text-left py-3 pr-6 font-semibold text-gray-900">Size</th>
            <th class="text-left py-3 pr-6 font-semibold text-gray-900">Font</th>
            <th class="text-left py-3 pr-6 font-semibold text-gray-900">Padding</th>
            <th class="text-left py-3 pr-6 font-semibold text-gray-900">Radius</th>
            <th class="text-left py-3 font-semibold text-gray-900">Use case</th>
          </tr>
        </thead>
        <tbody class="text-gray-600">
          <tr class="border-b border-gray-100">
            <td class="py-3 pr-6 font-mono text-xs">sm</td>
            <td class="py-3 pr-6 font-mono text-xs">text-sm (14px)</td>
            <td class="py-3 pr-6 font-mono text-xs">px-4 py-2</td>
            <td class="py-3 pr-6 font-mono text-xs">rounded-sm (4px)</td>
            <td class="py-3">Inline actions, table rows, tight layouts</td>
          </tr>
          <tr class="border-b border-gray-100">
            <td class="py-3 pr-6 font-mono text-xs">md</td>
            <td class="py-3 pr-6 font-mono text-xs">text-base (16px)</td>
            <td class="py-3 pr-6 font-mono text-xs">px-6 py-3</td>
            <td class="py-3 pr-6 font-mono text-xs">rounded-md (6px)</td>
            <td class="py-3">Default — forms, cards, general actions</td>
          </tr>
          <tr>
            <td class="py-3 pr-6 font-mono text-xs">lg</td>
            <td class="py-3 pr-6 font-mono text-xs">text-lg (18px)</td>
            <td class="py-3 pr-6 font-mono text-xs">px-8 py-4</td>
            <td class="py-3 pr-6 font-mono text-xs">rounded-md (6px)</td>
            <td class="py-3">Hero CTAs, landing page emphasis</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Icon Buttons -->
  <section class="specimen-section">
    <h2>With Icons</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      Icons sit inside the button's <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">gap-2</code> flex layout. Use trailing icons for forward navigation, leading icons for contextual actions. Icon size should match the font size tier.
    </p>
    <div class="border border-gray-200 rounded-lg p-8">
      <div class="flex flex-wrap gap-6 items-center">
        <div class="flex flex-col items-center gap-3">
          <Button>Next Trip <ArrowRight size={16} /></Button>
          <span class="text-xs font-mono text-gray-400">trailing icon</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button variant="secondary"><ArrowRight size={16} /> Back to Trips</Button>
          <span class="text-xs font-mono text-gray-400">leading icon</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button size="lg">Book Your Adventure <ArrowRight size={18} /></Button>
          <span class="text-xs font-mono text-gray-400">lg + icon</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button size="sm" variant="ghost"><Search size={14} /> Search Trips</Button>
          <span class="text-xs font-mono text-gray-400">sm ghost + icon</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Icon-Only Buttons -->
  <section class="specimen-section">
    <h2>Icon-Only</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      Icon-only buttons use the <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">iconOnly</code> prop, which switches to equal padding on all sides. Always pair with an accessible label via <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">aria-label</code> in production.
    </p>

    <h3 class="text-sm font-semibold text-gray-600 uppercase tracking-wide mb-4">By variant</h3>
    <div class="border border-gray-200 rounded-lg p-8 mb-6">
      <div class="flex flex-wrap gap-6 items-center">
        <div class="flex flex-col items-center gap-3">
          <Button iconOnly><Search size={16} /></Button>
          <span class="text-xs font-mono text-gray-400">search — primary</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button iconOnly variant="secondary"><Menu size={16} /></Button>
          <span class="text-xs font-mono text-gray-400">menu — secondary</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button iconOnly variant="ghost"><Close size={16} /></Button>
          <span class="text-xs font-mono text-gray-400">close — ghost</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button iconOnly><MapPin size={16} /></Button>
          <span class="text-xs font-mono text-gray-400">map pin — primary</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button iconOnly variant="secondary"><Calendar size={16} /></Button>
          <span class="text-xs font-mono text-gray-400">calendar — secondary</span>
        </div>
      </div>
    </div>

    <h3 class="text-sm font-semibold text-gray-600 uppercase tracking-wide mb-4">By size</h3>
    <div class="border border-gray-200 rounded-lg p-8">
      <div class="flex flex-wrap gap-8 items-end">
        <div class="flex flex-col items-center gap-3">
          <div class="flex gap-3">
            <Button iconOnly size="sm"><Search size={14} /></Button>
            <Button iconOnly size="sm" variant="secondary"><Menu size={14} /></Button>
            <Button iconOnly size="sm" variant="ghost"><Close size={14} /></Button>
          </div>
          <span class="text-xs font-mono text-gray-400">sm</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <div class="flex gap-3">
            <Button iconOnly><Search size={16} /></Button>
            <Button iconOnly variant="secondary"><Menu size={16} /></Button>
            <Button iconOnly variant="ghost"><Close size={16} /></Button>
          </div>
          <span class="text-xs font-mono text-gray-400">md (default)</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <div class="flex gap-3">
            <Button iconOnly size="lg"><Search size={18} /></Button>
            <Button iconOnly size="lg" variant="secondary"><Menu size={18} /></Button>
            <Button iconOnly size="lg" variant="ghost"><Close size={18} /></Button>
          </div>
          <span class="text-xs font-mono text-gray-400">lg</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Disabled -->
  <section class="specimen-section">
    <h2>Disabled</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      Disabled buttons get <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">opacity-50</code> and <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">cursor-not-allowed</code>. Works across all variants.
    </p>
    <div class="border border-gray-200 rounded-lg p-8">
      <div class="flex flex-wrap gap-6 items-center">
        <div class="flex flex-col items-center gap-3">
          <Button disabled>Book This Trip</Button>
          <span class="text-xs font-mono text-gray-400">disabled</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button variant="secondary" disabled>View Itinerary</Button>
          <span class="text-xs font-mono text-gray-400">disabled</span>
        </div>
        <div class="flex flex-col items-center gap-3">
          <Button variant="ghost" disabled>Back to Trips</Button>
          <span class="text-xs font-mono text-gray-400">disabled</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Button Pairs -->
  <section class="specimen-section">
    <h2>Common Pairings</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      Buttons rarely appear alone. Primary + secondary for decision points, primary + ghost for soft dismissal, size-matched pairs for visual balance.
    </p>

    <h3 class="text-sm font-semibold text-gray-600 uppercase tracking-wide mb-4">Hero CTA pair</h3>
    <div class="border border-gray-200 rounded-lg overflow-hidden mb-6">
      <div class="bg-gray-900 px-8 py-16 text-center">
        <div class="h-8 w-72 bg-gray-700 rounded-sm mx-auto mb-3"></div>
        <div class="h-4 w-96 bg-gray-800 rounded-sm mx-auto mb-8"></div>
        <div class="flex gap-4 justify-center">
          <Button size="lg">Book Your Adventure</Button>
          <Button size="lg" variant="secondary">
            <span class="text-white shadow-[inset_0_0_0_1px_rgba(255,255,255,0.2)]">View Itinerary</span>
          </Button>
        </div>
      </div>
    </div>

    <h3 class="text-sm font-semibold text-gray-600 uppercase tracking-wide mb-4">Form actions</h3>
    <div class="border border-gray-200 rounded-lg p-8 mb-6">
      <div class="max-w-md">
        <div class="h-4 w-20 bg-gray-200 rounded-sm mb-2"></div>
        <div class="h-10 w-full bg-gray-50 border border-gray-200 rounded-md mb-4"></div>
        <div class="h-4 w-24 bg-gray-200 rounded-sm mb-2"></div>
        <div class="h-10 w-full bg-gray-50 border border-gray-200 rounded-md mb-6"></div>
        <div class="flex gap-3 justify-end">
          <Button variant="ghost">Back to Trips</Button>
          <Button>Confirm Booking</Button>
        </div>
      </div>
    </div>

    <h3 class="text-sm font-semibold text-gray-600 uppercase tracking-wide mb-4">Card actions</h3>
    <div class="border border-gray-200 rounded-lg p-8 mb-6">
      <div class="max-w-sm bg-white rounded-lg border border-gray-200 overflow-hidden">
        <div class="h-40 bg-gray-100"></div>
        <div class="p-6">
          <div class="h-5 w-40 bg-gray-300 rounded-sm mb-2"></div>
          <div class="h-3 w-full bg-gray-100 rounded-sm mb-1"></div>
          <div class="h-3 w-4/5 bg-gray-100 rounded-sm mb-6"></div>
          <div class="flex gap-3">
            <Button size="sm">View Trip</Button>
            <Button size="sm" variant="ghost">Save Trip</Button>
          </div>
        </div>
      </div>
    </div>

    <h3 class="text-sm font-semibold text-gray-600 uppercase tracking-wide mb-4">Inline navigation</h3>
    <div class="border border-gray-200 rounded-lg p-8">
      <div class="flex items-center justify-between max-w-xl">
        <Button variant="secondary" size="sm">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
          Previous
        </Button>
        <span class="text-sm text-gray-400 font-mono">Trip 3 of 12</span>
        <Button variant="secondary" size="sm">
          Next
          <ArrowRight size={14} />
        </Button>
      </div>
    </div>
  </section>

  <!-- On Dark Backgrounds -->
  <section class="specimen-section">
    <h2>On Dark Backgrounds</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      Primary buttons work unchanged on dark surfaces. Secondary and ghost variants need inverted treatment — white text and white-based borders or hover states.
    </p>
    <div class="border border-gray-200 rounded-lg overflow-hidden">
      <div class="bg-gray-900 p-8">
        <div class="flex flex-wrap gap-4 items-center">
          <Button>Book This Trip</Button>
          <button class="inline-flex items-center justify-center gap-2 font-semibold cursor-pointer text-base px-6 py-3 rounded-md bg-transparent text-white shadow-[inset_0_0_0_1px_rgba(255,255,255,0.2)] hover:bg-white/10 active:bg-white/15 active:translate-y-px focus-visible:outline-2 focus-visible:outline-primary-300 focus-visible:outline-offset-2 transition-colors duration-fast ease-default">View Itinerary</button>
          <button class="inline-flex items-center justify-center gap-2 font-semibold cursor-pointer text-base px-6 py-3 rounded-md bg-transparent text-gray-400 hover:bg-white/10 hover:text-white active:bg-white/15 active:translate-y-px focus-visible:outline-2 focus-visible:outline-primary-300 focus-visible:outline-offset-2 transition-colors duration-fast ease-default">Back to Trips</button>
        </div>
      </div>
      <div class="bg-primary-600 p-8">
        <div class="flex flex-wrap gap-4 items-center">
          <button class="inline-flex items-center justify-center gap-2 font-semibold cursor-pointer text-base px-6 py-3 rounded-md bg-white text-primary-600 hover:bg-primary-50 active:bg-primary-100 active:translate-y-px focus-visible:outline-2 focus-visible:outline-white focus-visible:outline-offset-2 transition-colors duration-fast ease-default">Book This Trip</button>
          <button class="inline-flex items-center justify-center gap-2 font-semibold cursor-pointer text-base px-6 py-3 rounded-md bg-transparent text-white shadow-[inset_0_0_0_1px_rgba(255,255,255,0.3)] hover:bg-white/10 active:bg-white/15 active:translate-y-px focus-visible:outline-2 focus-visible:outline-white focus-visible:outline-offset-2 transition-colors duration-fast ease-default">View Itinerary</button>
          <button class="inline-flex items-center justify-center gap-2 font-semibold cursor-pointer text-base px-6 py-3 rounded-md bg-transparent text-primary-200 hover:bg-white/10 hover:text-white active:bg-white/15 active:translate-y-px focus-visible:outline-2 focus-visible:outline-white focus-visible:outline-offset-2 transition-colors duration-fast ease-default">Back to Trips</button>
        </div>
      </div>
    </div>
  </section>

  <!-- Anatomy -->
  <section class="specimen-section">
    <h2>Anatomy</h2>
    <p class="text-sm text-gray-600 mb-6 max-w-[65ch]">
      Class construction breakdown for the default primary button. The Astro component assembles these from <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">variant</code> and <code class="text-xs bg-gray-100 px-1.5 py-0.5 rounded-sm font-mono">size</code> props. All states use real pseudo-classes — hover, click, and tab to see them in the Demo section.
    </p>
    <div class="border border-gray-200 rounded-lg overflow-hidden">
      <div class="bg-gray-50 p-6 border-b border-gray-200">
        <div class="flex justify-center">
          <Button size="lg">Book Your Adventure</Button>
        </div>
      </div>
      <div class="p-6">
        <div class="flex flex-col gap-3 text-sm">
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Layout</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">inline-flex items-center justify-center gap-2</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Type</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">font-semibold cursor-pointer</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Size (sm)</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">text-sm px-4 py-2 rounded-sm (4px)</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Size (md)</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">text-base px-6 py-3 rounded-md (6px)</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Size (lg)</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">text-lg px-8 py-4 rounded-md (6px)</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Color</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">bg-primary-500 text-white</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Hover</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">hover:bg-primary-600</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Active</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">active:bg-primary-700 active:translate-y-px</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Focus</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">focus-visible:outline-2 focus-visible:outline-primary-300 focus-visible:outline-offset-2</code>
          </div>
          <div class="flex gap-3 items-start">
            <span class="text-xs font-semibold text-gray-500 uppercase tracking-wide w-20 shrink-0 pt-0.5">Motion</span>
            <code class="text-xs bg-gray-100 px-2 py-1 rounded-sm font-mono text-gray-700 leading-relaxed">transition-colors duration-fast ease-default</code>
          </div>
        </div>
      </div>
    </div>
  </section>

</GalleryLayout>
